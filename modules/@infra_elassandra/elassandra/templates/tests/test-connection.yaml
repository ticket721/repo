apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elassandra.fullname" . }}-test-connection"
  labels:
    {{- include "elassandra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "elassandra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
