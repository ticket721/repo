apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "platform.fullname" . }}--server-master--d
  labels:
    app: {{ include "platform.fullname" . }}--server
    name: {{ include "platform.fullname" . }}--server-master--d
    chart: {{ .Chart.Name }}
spec:
  replicas: {{ .Values.server.master.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: {{ include "platform.fullname" . }}--server
      name: {{ include "platform.fullname" . }}--server-master
      chart: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: {{ include "platform.fullname" . }}--server
        name: {{ include "platform.fullname" . }}--server-master
        chart: {{ .Chart.Name }}
    spec:
      serviceAccountName: {{ include "platform.fullname" . }}--platform--sa
      volumes:
        - name: server-config-feature-flags
          configMap:
            name: {{ template "platform.fullname" . }}--server-config-feature-flags--cfmp
        - name: server-config
          configMap:
            name: {{ template "platform.fullname" . }}--server-config-master--cfmp
        - name: server-secret
          secret:
            secretName: {{ template "platform.fullname" . }}--server-secret--sec
      initContainers:
        - name: {{ include "platform.fullname" . }}--server-master-wfj
          image: "groundnuty/k8s-wait-for:v1.3"
          args:
            - "job"
            - "{{ include "platform.fullname" . }}--migration-job"
          imagePullPolicy: {{ .Values.migrations.image.pullPolicy }}
      containers:
        - name: {{ template  "platform.fullname" . }}--server
          image: "{{ .Values.server.image.repository }}:{{ .Values.server.image.tag }}"
          imagePullPolicy: {{ .Values.server.image.pullPolicy }}
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "250m"
          volumeMounts:
            - name: server-config-feature-flags
              mountPath: /config/feature-flags.json
              subPath: feature-flags.json
            - name: server-config
              mountPath: /config/production.env
              subPath: production.env
            - name: server-secret
              mountPath: /config/production.secret.env
              subPath: production.secret.env
          env:
            - name: CONFIG_PATH
              value: /config/
            - name: NODE_ENV
              value: production
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
            timeoutSeconds: 10
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            timeoutSeconds: 10
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 30
