apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform.fullname" . }}--t721app--svc
  labels:
    name: {{ include "platform.fullname" . }}--t721app--svc
    chart: {{ .Chart.Name }}
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-backend-protocol: "tcp"
    service.beta.kubernetes.io/aws-load-balancer-proxy-protocol: "*"
spec:
  type: {{ .Values.t721app.service.type }}
  ports:
    - port: {{ .Values.t721app.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "platform.fullname" . }}--t721app
    chart: {{ .Chart.Name }}
