apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform.fullname" . }}--organizer--svc
  labels:
    name: {{ include "platform.fullname" . }}--organizer--svc
    chart: {{ .Chart.Name }}
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-backend-protocol: "tcp"
    service.beta.kubernetes.io/aws-load-balancer-proxy-protocol: "*"
spec:
  type: {{ .Values.organizer.service.type }}
  ports:
    - port: {{ .Values.organizer.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "platform.fullname" . }}--organizer
    chart: {{ .Chart.Name }}
